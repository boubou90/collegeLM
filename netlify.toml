[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Optional: redirect all routes to Astro (good for SPA-like behavior)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

